/**
 * infrastructure
 *
 * Provide common technical capabilities to other layers (such as tool classes, third-party library class support, common basic configurations, and underlying data access implementation)
 *
 * The basic implementation layer mainly includes the following contents:
 *
 * Deliver messages (such as notifications) to the application layer
 *
 * Provide persistence mechanism (lowest implementation) for the domain layer
 *
 * Provide component configuration for the user interface layer
 *
 */
package uk.shef.msc8.ewaste.infrastructure;